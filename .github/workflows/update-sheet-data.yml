#!/usr/bin/env python3
import csv
import re
import sys
from pathlib import Path

SHEET_URL = "https://docs.google.com/spreadsheets/d/1iK2SZUcz_ljnkdTG7KW6pqfzaUDuSgnlh1HupcLrkus/edit?gid=53744607"

HEADER_RE = re.compile(
    r"^(Early Access|Pre-Season|Season\b|Week\s*\d+\b)",
    re.IGNORECASE
)

def slug(s: str) -> str:
    s = s.strip().lower()
    s = re.sub(r"[^a-z0-9]+", "_", s)
    s = s.strip("_")
    return s or "section"

def lua_escape(s: str) -> str:
    s = s.replace("\\", "\\\\").replace('"', '\\"')
    s = s.replace("\r\n", "\n").replace("\r", "\n")
    return s

def is_section_header(text: str, row_index: int) -> bool:
    # The very first row is always a header in your sheet export
    if row_index == 0:
        return True
    return bool(HEADER_RE.match(text.strip()))

def main(csv_in: str, lua_out: str) -> None:
    csv_path = Path(csv_in)
    out_path = Path(lua_out)

    with csv_path.open("r", encoding="utf-8", newline="") as f:
        rows = list(csv.reader(f))

    sections = []
    current = None

    for i, row in enumerate(rows):
        a = (row[0] if len(row) > 0 else "").strip()
        if not a:
            continue

        if is_section_header(a, i):
            current = {"id": slug(a), "title": a, "items": []}
            sections.append(current)
        else:
            # ignore items before first header (shouldn't happen, but safe)
            if current is None:
                continue
            current["items"].append({"id": slug(a), "text": a})

    lines = []
    lines.append("-- Data file for Larias Weekly Midnight Checklist")
    lines.append("-- AUTO-GENERATED. DO NOT EDIT MANUALLY.")
    lines.append(f"-- Source: {SHEET_URL}")
    lines.append("")
    lines.append("local addonName = ...")
    lines.append("")
    lines.append('_G[addonName .. "_LIST_DATA"] = {')

    for s in sections:
        lines.append("    {")
        lines.append(f'        id = "{lua_escape(s["id"])}",')
        lines.append(f'        title = "{lua_escape(s["title"])}",')
        lines.append("        items = {")
        for it in s["items"]:
            lines.append(
                f'            {{ id = "{lua_escape(it["id"])}", text = "{lua_escape(it["text"])}" }},'
            )
        lines.append("        },")
        lines.append("    },")
    lines.append("}")
    lines.append("")

    out_path.parent.mkdir(parents=True, exist_ok=True)
    out_path.write_text("\n".join(lines), encoding="utf-8")

if __name__ == "__main__":
    if len(sys.argv) != 3:
        print("Usage: sheet_to_lua.py <input.csv> <output.lua>")
        sys.exit(2)
    main(sys.argv[1], sys.argv[2])
